Задание 1: Проверяемые исключения (Checked Exceptions)
Создайте собственное проверяемое исключение:

Создайте класс, который будет наследовать от Exception и назовите его,
например, InvalidAgeException. Добавьте в него конструктор, который принимает сообщение об ошибке.
Создайте класс Person:

Добавьте поле age (возраст) и метод setAge(int age), который устанавливает значение age.
В методе setAge(int age) выбрасывайте ваше InvalidAgeException, если age меньше 0 или больше 120.
Создайте тестовый класс:

В методе main создайте объект Person и попробуйте установить возраст, передавая
 разные значения (например, -1, 130, 25). Обработайте InvalidAgeException с помощью
 блока try-catch и выведите соответствующее сообщение.
 
 
 
 Задание 2: Непроверяемые исключения (Unchecked Exceptions)
 Создайте собственное непроверяемое исключение:
 
 Создайте класс, который будет наследовать от RuntimeException и назовите его, 
 например, InvalidInputException. Добавьте конструктор, который принимает сообщение об ошибке.
 Создайте класс Calculator:
 
 Добавьте метод divide(int a, int b), который выполняет деление a на b.
 В этом методе выбрасывайте ваше InvalidInputException, если b равен 0.
 Создайте тестовый класс:
 
 В методе main создайте объект Calculator и попробуйте выполнить деление, передавая 
 разные значения (например, 10 и 0, 10 и 2). Обработайте InvalidInputException с помощью 
 блока try-catch и выведите соответствующее сообщение.